# Project: DMD Video Separation

Applies Dynamic Mode Decomposition to split videos into low-rank background and sparse foreground components.

## Main entrypoint

```bash
python3 pre.py
```

The project contains source videos, generated AVI outputs, and a notebook (`impl.ipynb`).
